# evals/reports/

Aquí se guardan reportes generados por los pipelines de compilación y
evaluación (p. ej. `mipro_report.md` que produce `dspy_lab/optimize.py`).

El resto del contenido está gitignored; solo este README se versiona.
